spring:
  datasource:
    username: root
    password: 123
    url: jdbc:mysql://localhost:3306/mybatis_05?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    root: warn
    com.kwok: info
  file:
    name: blog-pro.log
    path: mylog/
server:
  port: 8081
